import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  basePath: '/BlazE-09X/Blockchain_projects/project7-Vote(2.0)/frontend-web', 
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
